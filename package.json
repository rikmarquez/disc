{
  "name": "disc-monolithic",
  "version": "0.2.0",
  "description": "Sistema de Encuestas DISC - Despliegue Monolítico",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../server && npm install && cd ..",
    "build": "npm run build:client && npm run build:server",
    "build:client": "cd client && npm install && npm run build && cd ..",
    "build:server": "cd server && npm install && npx prisma generate && npm run build && cd ..",
    "start": "cd server && npm start",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "prisma:migrate": "cd server && npx prisma migrate deploy",
    "prisma:seed": "cd server && npm run seed"
  },
  "keywords": ["disc", "encuesta", "perfilado", "conductual"],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=9.0.0"
  }
}
